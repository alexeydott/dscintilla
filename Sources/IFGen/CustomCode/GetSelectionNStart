function GetSelectionNStart(ASelection: Integer): NativeInt;
function %s.GetSelectionNStart(ASelection: Integer): NativeInt;
begin
  if (ASelection >= 0) and (ASelection < GetSelections) then
    Result := SendEditor(SCI_GETSELECTIONNSTART, ASelection, 0)
  else
    Result := INVALID_POSITION;
end;

