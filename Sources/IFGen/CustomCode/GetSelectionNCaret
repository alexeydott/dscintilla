function GetSelectionNCaret(ASelection: Integer): NativeInt;
function %s.GetSelectionNCaret(ASelection: Integer): NativeInt;
begin
  if (ASelection >= 0) and (ASelection < GetSelections) then
    Result := SendEditor(SCI_GETSELECTIONNCARET, ASelection, 0)
  else
    Result := INVALID_POSITION;
end;

